"""${message}

Revision ID: ${revision}
Down revision: ${down_revision}
Created: ${created}

Autogenerated by `langmigrate revision --autogenerate`. Review the body: defaults,
type coercions and possible renames need human judgement.
"""

from __future__ import annotations

from langmigrate import BaseMigration, StateEnvelope


class Migration(BaseMigration):
    revision = "${revision}"
    down_revision = ${down_revision_repr}
    slug = "${slug}"

    # Schema snapshot after this revision (baseline for the next autogenerate).
    fields = ${fields_repr}

    def upgrade(self, state: StateEnvelope) -> StateEnvelope:
${upgrade_body}
        return state

    def downgrade(self, state: StateEnvelope) -> StateEnvelope:
${downgrade_body}
        return state
